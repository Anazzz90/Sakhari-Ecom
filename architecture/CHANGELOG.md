# Architecture Documentation Changelog

Tracks changes to the `/architecture` documentation system itself — structural changes (new documents, renamed sections, changed conventions) — not the content decisions recorded inside individual documents or ADRs. Content-level history for a specific document lives in that document's own edit history (git blame) and in the ADRs that justified the change.

Newest entry first. Each entry: date, what changed, why.

## 2026-07-26 — Service Decomposition authored
Authored `03-decomposition/service-decomposition.md` in place of its prior skeleton — the fourth and final lens on the sixteen modules (alongside `module-catalog.md`'s technical, `capability-boundary-map.md`'s business, and `data-ownership-map.md`'s data lenses), this one internal: Public/Internal/Application/Domain Services and Repositories per module, elaborating `coding-standards.md` Section 3's generic "service layer" into the Application-vs-Domain-service distinction without introducing any new module, responsibility, or boundary. Documents why services exist, how they communicate (in-process within a module, Public-Service-only across modules), how they expose functionality, how they structurally enforce module boundaries, and how future extraction would work mechanically. Nine architectural rules (Thin Controllers, Business Logic Only in Services, No Repository Sharing, No Circular Dependencies, Public Interfaces Only, Domain Events, Transaction Ownership, Validation Boundaries, Error Propagation) plus a complete 16×16 service dependency matrix, mechanically derived from `module-catalog.md`'s Dependencies/Forbidden Dependencies fields with an explicit closed-world convention (undocumented = not sanctioned) rather than inventing any relationship. Closes with worked legal/illegal service-interaction examples and four future-extraction candidates (Order, Inventory, Auth, Search/Analytics) named without committing to a timeline. No code, no diagrams, no schema. `README.md` updated: purpose table entry and Current Status — this leaves only `glossary.md`, `observability-and-operations.md`, and `nfr-matrix.md` as remaining skeletons in the whole architecture set.

## 2026-07-26 — Data Ownership Map authored
Authored `03-decomposition/data-ownership-map.md` in place of its prior skeleton — the data-access lens completing the trio alongside `module-catalog.md` (technical) and `capability-boundary-map.md` (business-capability). For all sixteen modules: Owned Tables (conceptual, DDD-convention names, explicitly not schema), Owned Entities, Aggregate Roots (grounded in DDD §6's relationship philosophy, most directly its statement that "order is the central transactional aggregate, but inventory, payment, refund, and delivery retain module ownership"), Read/Write Responsibilities, Public Data Interfaces, data exposed through events and APIs, Forbidden Data Access, and Future Extensibility. Adds PostgreSQL/Redis ownership philosophy, why modules own data and why repositories are never shared, data consistency and transaction-ownership principles, a cross-module communication summary, and spotlight sections on Inventory/Order/User/Catalog/Payment ownership. Closes with a complete entity-level ownership matrix (owner, readers, modifiers, access method), allowed/forbidden access patterns, event-driven data sharing, a reporting/analytics/audit data section, seven Architecture Rules, and worked correct/incorrect ownership examples. No database schema (columns, types, constraints) described anywhere, per this task's instruction. Three items carried forward or newly surfaced are recorded in Open Decisions rather than resolved by invention: Settings' undeclared readers (carried from `capability-boundary-map.md`), the still-open refund-eligibility decision owner, and whether Auth's session/refresh-token state is a PostgreSQL table or Redis-backed state — genuinely unsettled by any prior document. `README.md` updated: purpose table entry and Current Status.

## 2026-07-26 — Capability Boundary Map authored
Authored `03-decomposition/capability-boundary-map.md` in place of its prior skeleton, against the now-settled sixteen-module catalog (ADR-0020) and the clarified checkout transaction model (ADR-0021). Provides the business-capability lens `module-catalog.md`'s technical lens doesn't carry: for each of the sixteen modules, a named Business Capability, Explicit Non-Responsibilities and Forbidden Responsibilities stated in business terms, Owned Business Processes, business-framed events, and Future Expansion Opportunities — plus Upstream/Downstream Dependencies summarized (not re-derived) from `module-catalog.md` and `module-communication.md`. Adds six top-level sections: Capability Boundary Principles, why capabilities are separated, worked examples of correct interactions and architecture violations, and a six-point process for proposing a future capability. Introduces no new module, responsibility, or boundary — per this task's instruction, every inconsistency found while cross-checking the sixteen modules' Dependencies fields (refund-eligibility's exact decision owner between Order and Support; Settings having no declared downstream dependents anywhere in `module-catalog.md`; Auth/Audit's informally-described "universally called" pattern) is recorded in its own Open Decisions section rather than resolved by invention. `README.md` updated: purpose table entry and Current Status.

## 2026-07-26 — Architecture review cleanup and ADR reconciliation
Cleaned up the architecture folder after review against the SRD/DDD and prior architectural discussion. Added ADR-0020 through ADR-0028 to close previously unresolved architecture gaps: module ownership reconciliation, checkout transaction-boundary clarification, Moyasar, Unifonic, AWS `me-central-2`, NestJS, JWT, OTP-first authentication, and CDN usage. Updated `decisions/README.md` accordingly.

Propagated ADR-0020 through the decomposition and cross-cutting documents: the backend module set is now sixteen modules including Support; Support Ticket/Comment belong to Support; Refund belongs to Payment; Picking belongs to Delivery; Workforce belongs to User; Price Snapshot belongs to Order; the Auth/User split is settled; and Search is accepted as a derived-read module. Also clarified ADR-0021's checkout model: Order orchestrates checkout, but module-owned persistence remains protected by module-local transactions and compensating actions rather than one oversized cross-module transaction.

Updated stale internal filename references from the earlier flat prompt structure to the actual nested `/architecture` layout, and propagated finalized provider/platform decisions from the SRD: Moyasar, Unifonic, AWS `me-central-2`, NestJS, JWT, OTP, and CDN. Remaining work is now clearly limited to still-skeleton documents and SDD-level implementation parameters, not unresolved module ownership.

## 2026-07-26 — AI Development Rules authored (ninth and final document of this documentation phase); new 08-ai-development/ folder
Authored `08-ai-development/ai-development-rules.md`, a genuinely new document with no prior skeleton, in a new `08-ai-development/` folder. Written specifically for AI coding assistants (not human onboarding, which is the ADS's job): required context to read before touching a given kind of task, a consolidated architecture-constraints checklist, forbidden patterns, prompting guidelines for the assistant's own behavior, module ownership/transaction/security rules, testing expectations, a review-checklist pointer to `coding-standards.md`, a definition of done, and — the most novel content in the document — two named catalogs of failure modes specific to this project: Common AI Hallucinations (including a direct citation of this project's own ULID/UUID incident, ADRs 0013/0018/0019, as a concrete worked example) and Common Architectural Violations. Introduces no new architectural content; every rule is a pointer to or restatement of a rule established in full elsewhere.

This completes the nine-document phase requested across this and the prior several turns (Module Catalog through AI Development Rules). `README.md` was updated throughout each step and, with this entry, now points to `08-ai-development/ai-development-rules.md` as the recommended starting point for "what do I read before writing code" (Section 9), with the underlying policy in that same section unchanged. Three folders (`03-decomposition/`, `04-cross-cutting/`, `05-deployment/`) now mix fully-authored documents with a handful of still-skeleton ones (`glossary.md`; `capability-boundary-map.md`, `service-decomposition.md`, `data-ownership-map.md`; `observability-and-operations.md`; `06-quality-attributes/nfr-matrix.md`) — the next natural phase of work, not part of this request.

## 2026-07-26 — Coding Standards authored (eighth of the new documentation phase); new 07-coding-standards/ folder
Authored `07-coding-standards/coding-standards.md`, a genuinely new document with no prior skeleton, in a new `07-coding-standards/` folder — the structure previously proposed and approved for this and the upcoming AI Development Rules document. Expands `00-Architecture-Principles.md` Section 8 (Coding Philosophy) into concrete, checkable practice: repository and module structure (module directories mirroring `module-catalog.md` exactly, with data-access code unreachable from outside its own module — the structural expression of ADR-0009), naming conventions (code vocabulary matches documentation vocabulary exactly), logging, validation, and error-handling standards, testing philosophy (tests protect business rules and are weighted toward correctness-critical and idempotency-sensitive paths), documentation standards, a code review checklist, and a separate architecture-compliance checklist. Deliberately does not mandate a specific test framework, linter, repository topology (monorepo vs. polyrepo), or coverage threshold — none of these are decided in any prior document, and each is named explicitly in its own Open Decisions section rather than invented. `README.md` updated: new folder in the tree and purpose table, a tenth step added to Order of Authorship, and a stability-table entry.

## 2026-07-26 — Performance & Scalability authored (seventh of the new documentation phase)
Authored `04-cross-cutting/reliability-and-performance.md` in place of its prior skeleton, retitled to cover both its original scope (availability targets, latency budgets, failure-handling strategy) and the requested Performance/Scalability content: performance philosophy, caching strategy (Redis vs. CDN, why two layers), search strategy (Search module's index as a performance mechanism, not just a feature), read/write optimization (Analytics and Search removing read load architecturally before read replicas are considered), queue usage (event dispatch and background jobs both built on infrastructure already in the stack, no new queue technology introduced), horizontal scaling (stateless JWT-based requests and shared Redis coordination as the properties that make it straightforward when needed), database scaling (read replicas for read load, partitioning for append-only table growth — two distinct mechanisms for two distinct problems), future microservice extraction (confirming Order/Inventory as the named candidates from `module-catalog.md` without repeating the reasoning), and capacity planning principles (size for the business that exists, monitor before scaling, prefer the cheapest sufficient lever). Carries its own Open Decisions section (exact SLA numbers, specific scaling trigger thresholds, partitioning specifics — all explicitly deferred elsewhere already). `README.md` updated accordingly.

## 2026-07-26 — Deployment Architecture authored (sixth of the new documentation phase)
Authored both `05-deployment/environment-strategy.md` and `05-deployment/infrastructure-and-release.md` in place of their prior skeletons, together covering the requested Deployment Architecture scope split logically across the two existing files: `environment-strategy.md` covers Production Environment (a three-environment model — Development/Staging/Production — deliberately not larger, given the one-developer constraint), what varies per environment (external-integration sandbox-vs-live mode being the load-bearing variation), and the promotion path; `infrastructure-and-release.md` covers Infrastructure Components, Networking, Storage, Cache, Logging, Monitoring, Backup, Disaster Recovery, CI/CD, and a Scaling Strategy overview (deliberately kept light, deferring to `04-cross-cutting/reliability-and-performance.md`).

Per this task's own instruction, cloud-provider and infrastructure details were kept abstract everywhere no prior document had already decided them (specific compute service, network topology, exact backup retention/RPO/RTO, specific CI/CD tool) — each named explicitly in Open Decisions rather than invented. This also resolves `data-architecture.md` Section 14's explicitly deferred RPO/RTO framing: the mechanism (managed automated backups, point-in-time recovery, single-region-with-disciplined-backups) is now established, though specific numeric targets remain open. `README.md` updated accordingly.

## 2026-07-26 — Event Architecture authored (fifth of the new documentation phase)
Authored `04-cross-cutting/integration-and-messaging.md` in place of its prior skeleton, retitled Event Architecture and covering the asynchronous/event half of that file's original scope in full: domain events, event naming, publisher/consumer rules, background processing (mapping all seven DDD-named background jobs to their relationship with the event system), the complete notification flow, retry strategy (publish-failure vs. consumption-failure, handled differently), idempotency as the baseline assumption for every consumer, event versioning (mirroring ADR-0017's API-versioning discipline), and the future event-bus strategy for eventual module extraction. Finally resolves the Audit invocation-mechanism question carried forward from `module-communication.md` and `data-architecture.md`: Audit consumes existing domain events where one already exists for other reasons, and receives a direct synchronous call only where no natural event exists (e.g., viewing sensitive data). The original skeleton's other two sections (synchronous API conventions, external-integration retry/circuit-breaking patterns) and its integration-inventory section are explicitly not covered here and are named in this document's own Open Decisions rather than silently dropped. `README.md` updated accordingly.

## 2026-07-26 — Security Architecture authored (fourth of the new documentation phase)
Authored `04-cross-cutting/security-and-compliance.md` in place of its prior skeleton, retitled Security Architecture while retaining its original SAMA/PDPL compliance-mapping scope. Covers identity (the Auth/User split), OTP-based authentication (and its reuse for rider delivery-confirmation), JWT's architectural role, refresh-token rotation with reuse detection, RBAC and the fine-grained permission model (ADR-0012), API security (validation at the boundary, Redis-backed rate limiting, symmetric trust treatment of external responses), audit's security framing (distinct from but complementary to `data-architecture.md`'s data-mechanics treatment of the same module), secrets handling (JWT signing keys as the highest-blast-radius secret; per-module external credential isolation), and a compliance-obligation-to-control mapping table. Carries its own Open Decisions section (exact token lifetimes, the full permission list, Support-module permission scope pending `module-catalog.md`'s Open Decision 1). `README.md` updated accordingly.

## 2026-07-26 — Data Architecture authored (third of the new documentation phase)
Authored `04-cross-cutting/data-architecture.md` in place of its prior skeleton (rather than creating a new file), since its existing scope already matched what was requested. Covers PostgreSQL and Redis philosophy, data ownership, transactions, the inventory model (Inventory Item / Reservation / Ledger as three distinct entities), the reservation lifecycle (requested → reserved → consumed/released-or-expired), the inventory ledger's role as history explaining current state, soft-delete vs. immutable vs. state-driven entity treatment, and the practical (not re-argued) consequences of the ULID, UTC, and integer-halala-money ADRs. Concurrency strategy (locking, optimistic validation, idempotency, named race conditions) and a data-level recovery strategy (compensating actions, reconciliation jobs, the ledger/audit trail itself) are both grounded directly in DDD Sections 9–15, with infrastructure-level backup/DR explicitly scoped out to the future `05-deployment/infrastructure-and-release.md`. Partially resolves `module-communication.md`'s open question on Audit's invocation mechanism (confirmed as a push model per DDD Section 12.1; the exact call-vs-event mechanism remains open for `04-cross-cutting/integration-and-messaging.md`). Carries its own Open Decisions section (deadlock-avoidance ordering, Audit's exact push mechanism, archival timing, retention-window specifics). `README.md` updated accordingly.

## 2026-07-26 — Module Communication authored (second of the new documentation phase)
Authored `03-decomposition/module-communication.md`: the four allowed forms of communication (client-to-backend REST, in-process module interface calls, domain events, module-to-external-system calls) and their forbidden counterparts; the full inter-module dependency graph as five layers (Foundation, Reference, Commerce, Transaction Orchestration, Fulfillment, Consumer tier), built from `module-catalog.md`'s per-module dependency fields; transaction-boundary rules establishing that no transaction ever spans two modules (cross-module consistency is achieved by orchestration with explicit compensation, not distributed transactions); and circular-dependency prevention, including the event mechanism's role as the designed escape valve for relationships that would otherwise require an upward dependency.

This resolves two points `module-catalog.md` had left open: (1) Payment initiation is a direct synchronous call from Order, not event-triggered — `module-catalog.md`'s Payment entry was given a small corrective edit to match; (2) the tension between ADR-0010's "single transaction" wording and the DDD's two-transaction model (Section 9.1/9.2) is resolved in favor of the DDD's model plus an orchestration-with-compensation pattern, with a note that ADR-0010's wording should eventually be tightened by a clarifying ADR. `README.md`'s folder tree and purpose table were updated accordingly.

## 2026-07-26 — Module Catalog authored (first of a new documentation phase)
Authored `03-decomposition/module-catalog.md`, the first of a planned nine-document phase (Module Catalog, Module Communication, Data Architecture, Security Architecture, Event Architecture, Deployment Architecture, Performance/Scalability, Coding Standards, AI Development Rules) requested against a fifteen-module backend list (Auth, User, Store, Catalog, Search, Inventory, Cart, Order, Payment, Promotion, Delivery, Notification, Analytics, Audit, Settings). Per the requester's own instruction, new documents are being slotted into the existing nested folder structure rather than flattened to a new root-level numbering scheme, to preserve the current structure and its cross-references.

The fifteen-module list does not fully match the DDD's nineteen-module-owner breakdown or the ADS's nine-capability grouping — most notably, the DDD's Support, Refund, Picking, Workforce, and Pricing modules have no direct counterpart, and Search is entirely new. Rather than resolve these silently, `module-catalog.md` documents seven specific reconciliation points in its own Open Decisions section (Section 3) and applies working assumptions consistently throughout, flagged as unconfirmed. `capability-boundary-map.md`, `service-decomposition.md`, and `data-ownership-map.md` were each given a short note pointing to `module-catalog.md` as the new primary per-module source of truth, to be summarized rather than duplicated when those three are eventually authored. `README.md`'s folder tree and purpose table were updated accordingly.

## 2026-07-26 — Technology Decisions authored
Authored `04-cross-cutting/technology-decisions.md`, documenting every technology in current use — React Native, Next.js, React/Vite (Admin Dashboard), NestJS, PostgreSQL, Redis, S3, JWT, OTP, Cloud Infrastructure (AWS), and CDN — each with Purpose, Why Chosen, Alternatives Considered, Benefits, Drawbacks, and Future Replacement Strategy. This expands the ADS's Section 9 (Technology Decisions) into full detail and is the connective layer between that category-level summary and the individual ADRs: it cross-references rather than duplicates the six technologies that already have a dedicated ADR (PostgreSQL — 0003, Redis — 0004, React Native — 0006, Next.js — 0007, React/Vite — 0008, Object Storage — 0016), and names five technologies with no dedicated ADR yet (NestJS, JWT, OTP, Cloud Infrastructure provider specifics, CDN) as backfill candidates, consistent with the pattern used for ADRs 0002–0017. `README.md`'s folder tree, purpose table, and Current Status were updated accordingly.

## 2026-07-26 — ADR-0018 corrected: ULIDs restored (ADR-0019)
The project owner confirmed that ULID, not UUID, was always the intended primary-key decision — the DDD's UUID text (which ADR-0018 had reasoned from) was itself the error. Corrected `DDD_Sakhari_Ecom_v1.0.md` Sections 1.2, 2.3, and 3.1 back to ULID, with rationale text updated to treat lexicographic sortability as a benefit rather than a leak. Authored `decisions/0019-restore-ulid-primary-keys.md` to supersede ADR-0018 rather than silently editing it, preserving the full decision history (0013 ULID → 0018 UUID, based on a draft DDD error → 0019 ULID, based on the corrected DDD) per the ADR immutability rule in `decisions/README.md`. ADR-0018's Status line was updated to `Superseded by 0019`; the index and this changelog reflect the current effective decision (ADR-0019, ULIDs).

## 2026-07-26 — System Context authored; DDD cross-checked; ADR-0013 superseded
Authored `02-context/system-context.md` in full, expanding the ADS's Section 6 into a complete system-context model across four zones — Customer Platform, Operations Platform, Backend, Infrastructure — each with Responsibilities, Boundaries, Trust Boundaries, External Integrations, and Internal Integrations, plus a consolidated "Diagram-Ready Model" (Nodes/Edges tables) so a future C4 Level 1/2 diagram can be generated directly from it without re-deriving the model. "Operations Platform" is introduced as this document's collective name for the Worker App + Admin/Ops Dashboard, mirroring the ADS's existing "Customer Platform" term.

This is the first document authored after `DDD_Sakhari_Ecom_v1.0.md` became available. Cross-checking against it surfaced two real findings, both incorporated: (1) the DDD confirms two external systems not previously named anywhere in the architecture set — a Push Notification Service and a Geocoding/Maps Provider (DDD Section 5.3) — now added to `system-context.md` with explicit notes on what's confirmed vs. still open (Worker App push, and whether geocoding is client-embedded or backend-mediated); (2) the DDD's Section 1.2/2.3/3.1 specifies UUID primary keys, directly conflicting with ADR-0013's prior choice of ULIDs (authored before the DDD existed). Rather than leave that contradiction standing, `decisions/0018-uuid-primary-keys.md` was authored to supersede ADR-0013 per the ADR lifecycle rules; `0013`'s Status line was updated to `Superseded by 0018` (its Decision/Rationale content left untouched, consistent with ADR immutability), and both `decisions/README.md`'s index and this changelog reflect the change.

## 2026-07-24 — First 16 substantive ADRs backfilled (0002–0017)
Authored ADR-0002 through ADR-0017, backfilling the significant technical decisions already implicit in the finalized SRD, the Constitution, and the ADS — closing the outstanding-work note in `decisions/README.md` and the ADS's Section 8/9. Covers: modular monolith over microservices (0002), PostgreSQL as single source of truth (0003), Redis as cache/coordination only (0004), the TypeScript-centered stack (0005), React Native for the Customer Mobile and Worker apps (0006), Next.js for the Customer Web App (0007), the React/Vite Admin Dashboard (0008), module ownership and the ban on cross-module repository access (0009), transactional checkout and inventory reservation (0010), event-driven asynchronous side effects (0011), RBAC with fine-grained permissions (0012), ULIDs for entity identifiers (0013), UTC timestamp storage (0014), integer halala money representation (0015), object storage for files and images (0016), and versioned APIs from day one (0017). `decisions/template.md` was extended with three fields these substantive ADRs required beyond the original process-decision template — Problem, Future reconsideration conditions, and Related architecture principle(s) — so all future ADRs follow the same richer structure; `decisions/0001` was left in its original form rather than retrofitted, since editing an Accepted ADR's structure after the fact is exactly the kind of silent rewrite the ADR process itself exists to prevent. `decisions/README.md`'s index was updated with all 17 records.

## 2026-07-24 — ADS authored; detail folders renumbered to 02–06
Authored `01-Architecture-Design-Specification.md` — the master, single-source-of-truth description of the architecture (business context, system vision, architectural goals, platform overview, system context, high-level architecture, architectural decisions summary, technology decisions, module overview, and the data/security/scalability/reliability philosophies), realizing the Constitution's principles into an actual system shape by reference rather than repetition. Because the ADS sits directly beneath the Constitution in the documentation hierarchy, the previously-numbered detail folders shifted down one position to make room for it: `01-context/` → `02-context/`, `02-decomposition/` → `03-decomposition/`, `03-cross-cutting/` → `04-cross-cutting/`, `04-deployment/` → `05-deployment/`, `05-quality-attributes/` → `06-quality-attributes/`. All cross-references in `README.md`, `diagrams/README.md`, `06-quality-attributes/nfr-matrix.md`, and `04-cross-cutting/reliability-and-performance.md` were updated to match. This is the second content document (after the Constitution) to reach fully-authored, stable-shape status.

## 2026-07-24 — Constitution authored; `00-charter/` retired
Authored `00-Architecture-Principles.md` — the Constitution of the Architecture (engineering philosophy, the twelve core architectural principles, design goals/tradeoffs/constraints, coding and AI-development philosophy, anti-patterns, decision-making guidelines, and amendment process). This single top-level document fully absorbs the scope originally split across the `00-charter/charter.md` and `00-charter/principles.md` skeletons, so `00-charter/` was removed and all cross-references updated to point at `00-Architecture-Principles.md`. This is the first content document in `/architecture` other than a process ADR to reach fully-authored, Stable status.

## 2026-07-24 — Initial structure established
Created the `/architecture` folder structure (charter, principles, context, decomposition, cross-cutting concerns, deployment, quality attributes, ADRs, diagram conventions) as the documentation layer between the Database Design Document (DDD) and implementation, per the SRD's finalized application/backend shape (v2.6). All content documents are skeletons (purpose, scope, and relationships defined; architectural content not yet authored) — see each document's `Status` line.

